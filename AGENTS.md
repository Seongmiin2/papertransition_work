# 프로젝트 에이전트 작업 지침

이 파일은 이 저장소에서 코드를 작성·수정·리뷰하는 AI 에이전트에 적용된다.

- 원본: https://github.com/multica-ai/andrej-karpathy-skills
- 기준 리비전: `2c606141936f1eeef17fa3043a72095b4765b9c2`
- 원본 라이선스 표기: MIT (`README.md`, 플러그인 manifest 기준)
- 통합 범위: `CLAUDE.md`, `SKILL.md`, Cursor 규칙, 예제와 사용 문서의 실질적인 내용을 중복 없이 정리했다.

이 지침은 신중함을 속도보다 약간 우선한다. 다만 오탈자나 명백한 한 줄 수정까지 긴 계획이나 광범위한 검증을 요구하지 않는다.

## 1. 코딩 전에 생각한다

가정한 내용을 숨긴 채 구현을 시작하지 않는다.

- 요구사항과 현재 코드에서 직접 확인할 수 있는 사실을 먼저 구분한다.
- 결과가 달라질 수 있는 중요한 가정만 명시한다.
- 해석이 여러 개이고 선택에 따라 결과가 크게 달라지면 선택지와 장단점을 짧게 제시한다.
- 더 단순한 해법이 있으면 먼저 알린다.
- 코드와 자료에서 답을 확인할 수 있으면 사용자에게 되묻지 말고 확인한다.
- 불확실성이 구현 방향을 바꿀 정도일 때만 멈추고 질문한다.

### 알고리즘 점검

비단순한 알고리즘을 구현하기 전에 다음을 한 번 확인한다.

1. 입력, 출력, 핵심 불변 조건이 무엇인가?
2. 기존 데이터 흐름에서 어느 단계가 책임져야 하는가?
3. 새 추상화 없이 해결할 수 있는 가장 단순한 방법은 무엇인가?
4. 시간·메모리 비용과 실제 입력 크기에 맞는가?
5. 빈 입력, 경계값, 실패 시 동작 중 실제로 가능한 것은 무엇인가?

점검 결과가 명확하면 바로 구현한다. 생각을 길게 늘어놓기 위한 의식으로 만들지 않는다.

## 2. 단순함을 우선한다

오늘 필요한 문제를 푸는 최소한의 코드를 작성한다.

- 요청하지 않은 기능을 추가하지 않는다.
- 한 번만 쓰는 코드를 위해 계층, 전략 패턴, 팩토리 또는 설정 체계를 만들지 않는다.
- 필요성이 확인되지 않은 확장성이나 유연성을 미리 넣지 않는다.
- 실제로 발생할 수 없는 상황을 위한 방어 코드를 쌓지 않는다.
- 200줄을 50줄로 명확하게 표현할 수 있다면 단순화한다.
- 복잡성이 실제 요구사항으로 생겼을 때 리팩터링한다.

판단 기준: 숙련된 개발자가 현재 요구사항에 비해 과도하다고 볼 구조라면 줄인다.

## 3. 필요한 부분만 정확히 바꾼다

모든 변경 줄은 사용자 요청이나 그 변경으로 생긴 필수 후속 작업과 연결되어야 한다.

- 관련 없는 코드, 주석, 이름 또는 서식을 함께 고치지 않는다.
- 고장 나지 않은 주변 코드를 임의로 리팩터링하지 않는다.
- 개인 취향보다 기존 코드 스타일을 따른다.
- 현재 변경 때문에 사용되지 않게 된 import, 변수, 함수는 제거한다.
- 기존의 미사용 코드는 사용자가 정리를 요청했을 때만 제거한다.
- 이해하지 못한 코드나 주석은 삭제하지 않는다.
- 관련 없는 문제를 발견하면 변경하지 말고 결과에 짧게 기록한다.

작업이 끝나면 diff를 보고 요청과 무관한 변경이 섞였는지 확인한다.

## 4. 성공 조건으로 작업한다

모호한 명령을 확인 가능한 완료 조건으로 바꾼다.

- 버그 수정: 재현 조건을 확인하고 그 조건이 사라졌는지 확인한다.
- 기능 추가: 입력, 기대 출력, 주요 실패 조건을 먼저 정한다.
- 리팩터링: 외부 동작이 유지되고 구조가 실제로 단순해졌는지 확인한다.
- 성능 개선: 개선하려는 시간 또는 메모리 병목을 특정하고 전후를 같은 조건으로 비교한다.

여러 단계가 필요한 작업에는 다음 형식의 짧은 계획만 사용한다.

```text
1. 변경할 책임 경계 확인 → 확인: 관련 호출 흐름 파악
2. 최소 구현 → 확인: 대표 입력에서 기대 결과
3. 회귀 확인 → 확인: 관련 테스트 통과 및 diff 검토
```

## 5. 검증은 위험에 비례한다

검증 자체를 목표로 삼지 않는다. 변경이 깨뜨릴 수 있는 범위만큼 확인한다.

- 오탈자·문서 수정: 내용과 diff 확인으로 충분하다.
- 국소 로직 수정: 해당 단위 테스트와 관련 정적 검사만 실행한다.
- 공용 API·데이터 모델·파이프라인 변경: 관련 통합 테스트까지 실행한다.
- 알고리즘·성능 변경: 대표 실제 입력 한 건과 핵심 경계값을 확인한다.
- 설치, 배포, 데이터 삭제처럼 영향이 큰 작업은 실행 전에 대상과 복구 가능성을 확인한다.

전체 테스트를 반복 실행하거나 같은 사실을 여러 방식으로 증명하지 않는다. 실패가 발생하면 원인을 좁힌 뒤 필요한 검사만 다시 실행한다.

## 6. 좋은 요청을 만드는 방법

에이전트에는 구현 방법만 나열하기보다 성공 조건과 제약을 함께 준다.

### 버그 수정

```text
[현상] 입력 X에서 Y가 발생한다.
[완료 조건] 입력 X가 Z를 반환하고 기존 A 동작은 유지된다.
[범위] 관련 모듈만 수정하고 공개 API는 바꾸지 않는다.
```

### 리팩터링

```text
[목표] X 흐름의 중복과 메모리 사용을 줄인다.
[완료 조건] 출력은 동일하고 중복 경로는 하나로 합쳐진다.
[제약] 새 프레임워크나 범용 추상화는 추가하지 않는다.
```

### 새 기능

```text
[입력/출력] 무엇을 받아 무엇을 만들어야 하는지 적는다.
[완료 조건] 대표 사례와 실패 사례를 적는다.
[제약] 성능, 호환성, 변경 가능한 파일 범위를 적는다.
```

## 7. 적용 여부 확인

다음 변화가 보이면 이 지침이 제대로 작동하는 것이다.

- diff에 요청하지 않은 변경이 줄어든다.
- 한 번만 쓰는 추상화와 추측성 옵션이 줄어든다.
- 중요한 모호함은 구현 전에 드러나고 사소한 일은 바로 처리된다.
- 완료 보고에 실제 성공 조건과 확인 결과가 간결하게 남는다.
- 코드가 미래의 가상 문제보다 현재 문제를 이해하기 쉽게 해결한다.

